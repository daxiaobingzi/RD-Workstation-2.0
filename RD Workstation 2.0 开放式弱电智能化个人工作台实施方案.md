# RD Workstation 2.0
## 开放式弱电智能化个人工作台——总体设计与具体实施方案

> 版本：V2.2 产品与技术实施基线
>
> 定位：个人弱电智能化设计师的高效、开放、可扩展桌面工作台
>
> 核心原则：**少录入、少重复、强复用、可自定义、自动联动、专业能力沉淀**

---

# 1. 项目背景与重新定位

RD Workstation 已经经过 V1 和 V2.1 两轮验证。V1 的核心价值在于，它已经把真实弱电设计流程跑通了一部分：项目、子系统、点位、设备、数量推导、设备选型、清单、价格、模板等能力已经形成业务雏形；V2.1 则完成了 React + TypeScript 方向以及工程目录、Engine、Service、Repository 等架构收敛。

但是在 V2.1 Review 中发现一个更重要的问题：**技术架构正在变好，但产品模型有逐渐“封闭化”的趋势。**

表现为：

- 设计中心预设了较固定的字段结构；
- 设备中心倾向于按传统产品数据库方式要求用户录入大量结构化字段；
- 设计推导规则更多是程序预设，而不是用户可视化定义；
- 智能选型逻辑也更偏“系统规定怎么选”，而不是“设计师定义怎么选”；
- 数据录入成本可能高于使用软件本身节省的时间。

因此，V2.2 不再追求“把数据库做得越来越精细”，而是将产品重新定位为：

> **一个由设计师自己组织设计方法、设备资料和推导规则，同时尽可能少做重复录入的开放式弱电设计平台。**

---

# 2. 产品核心理念

## 2.1 六条最高优先级原则

### 原则一：能自动的不要人工
系统应尽量自动完成汇总、计算、联动、生成和校验。

### 原则二：能复制的不要重新录入
供应商资料、历史项目、旧点表、旧设备资料应尽可能直接复制或导入。

### 原则三：能批量的不要逐条操作
点位、设备、价格、清单必须支持批量导入、批量编辑、批量替换。

### 原则四：结构化只发生在“机器真正需要计算”的地方
设备参数允许保留为完整文本内容，不要求设计师把供应商资料拆成几十个字段。

### 原则五：项目结果与基础资料分离
设备库里是什么设备，与某个项目用了多少台完全是两件事。

### 原则六：用户定义工作方法，软件负责执行
系统可以提供默认模板，但任何核心设计模型、字段内容、推导规则、选型策略都不能依赖程序员修改代码。

---

# 3. 最终产品结构

一级功能建议正式冻结为：

```text
01 今日
02 项目
03 设计
04 设备
05 清单预算
06 设计工具
07 知识
08 目标
09 习惯
10 AI
11 模型与规则
12 设置
```

其中“模型与规则”是 V2.2 新增的核心管理入口，但其目标不是让用户编程，而是让用户建立自己的设计方法。

---

# 4. 系统整体业务闭环

```text
                 ┌───────────────┐
                 │    今日工作台   │
                 └───────┬───────┘
                         │
                 ┌───────▼───────┐
                 │      项目       │
                 └───────┬───────┘
                         │
                 ┌───────▼───────┐
                 │      系统       │
                 └───────┬───────┘
                         │
                 ┌───────▼───────┐
                 │      点位       │
                 └───────┬───────┘
                         │
           ┌─────────────┼─────────────┐
           ▼             ▼             ▼
        设备名称         规则          设计内容
           │             │
           └─────────────┼─────────────┘
                         ▼
                     设备推导
                         ▼
                     智能选型
                         ▼
                 数量 / 单价 / 合价
                         ▼
                    工程量 / 清单
                         ▼
                       预算
                         ▼
                 校核 / 成果 / 复盘
                         ▼
                    知识 / 模板
```

个人工作流：

```text
目标 → 任务 → 日程 → 今日执行 → 完成 → 复盘
                      ↑
                   项目工作
```

---

# 5. 项目中心重新设计

## 5.1 项目层级

因为实际工程存在多建筑、多区域、多弱电间，项目结构统一设计为：

```text
项目
├── 建筑
│   └── 弱电间
│       ├── 1F弱电间
│       ├── 2F弱电间
│       └── ……
├── 系统
├── 任务
├── 日程
├── 清单
├── 预算
├── 文档
└── 复盘
```

建议增加三类基础对象：

- Building：建筑
- TelecomRoom：弱电间

点位不直接保存自由文本的全部层级关系，而是通过关联选择建筑和弱电间。

## 5.2 项目主页

项目主页重点展示：

```text
项目基本信息
项目总体进度
系统进度
今日任务
近期日程
预算
设计问题
最近修改
```

项目进度不鼓励人工输入，应逐步根据系统任务、设计对象、清单等数据自动计算。

---

# 6. 设计中心——从“固定字段”改成“轻量设计工作表”

这是本次产品调整的核心。

## 6.1 系统设计工作区

每个系统统一提供：

```text
概览
设计内容
点位
设备
选型
推导
拓扑
工程量
清单
预算
校核
说明
```

但不要求每一个系统必须填写完全一样的数据。

系统可以提供“推荐流程”，设计师可以隐藏或显示部分环节。

---

# 7. 点位模型——正式冻结

## 7.1 点位最小字段

按照本次确认，点位只保留核心字段：

```text
设备名称
区域
弱电间
数量
```

建议增加一个系统自动生成但用户不必填写的：

```text
点位ID / 点位序号
```

### 设备名称
必须链接设备中心的“设备名称”，不能是完全自由文本。

使用方式：

```text
点击设备名称
→ 输入关键字
→ 模糊搜索
→ 推荐结果
→ 最近使用
→ 收藏
→ 当前系统设备
```

搜索示例：

```text
摄像 → 网络摄像机 / 枪机 / 半球 / 球机
交换 → 接入交换机 / POE交换机 / 汇聚交换机
海康 400 → 海康400万相关设备
```

## 7.2 建筑、区域、弱电间选择

区域结构：

```text
项目
→ 建筑
→ 区域
→ 弱电间
```

例如：

```text
A栋
 ├ 大厅
 ├ 走廊
 ├ 办公区
 └ 地下车库

弱电间：
 ├ 1F-IDF
 ├ 3F-IDF
 └ 6F-IDF
```

点位录入界面不展开复杂层级，而采用联动下拉：

```text
建筑：A栋
区域：3F办公区
弱电间：3F-IDF
```

## 7.3 点位批量操作

必须支持：

```text
复制行
批量新增
批量删除
批量移动区域
批量移动弱电间
批量修改设备名称
Excel导入
复制粘贴
```

---

# 8. 设备中心——极简模型

这是本项目区别于普通设备管理软件的第二个核心。

## 8.1 设备主数据只负责回答“这是什么”

设备最小模型：

```text
设备名称
设备类别
单位
通用参数
```

其中：

> **通用参数只有一个“大内容框”。**

例如：

```text
400万网络高清摄像机，1/2.7英寸CMOS，
支持H.265，支持PoE供电，IP67，
支持人形检测、车辆检测……
```

用户可以直接从供应商官网、产品手册、微信、报价单、Excel 等处复制粘贴。

## 8.2 型号信息

每个设备可以有多个品牌/型号。

```text
品牌
型号
型号参数
单价
```
其中：

> **型号参数只有一个“大内容框”。**

例如：

```text
400万网络高清摄像机，1/2.7英寸CMOS，
支持H.265，支持PoE供电，IP67，
支持人形检测、车辆检测……
```
用户可以直接从供应商官网、产品手册、微信、报价单、Excel 等处复制粘贴。

## 8.3 数量与合价不属于设备库

绝对不在设备主数据中维护：

```text
数量
合价
```

原因：

```text
设备：网络摄像机
项目A：120台
项目B：386台
项目C：52台
```

数量属于项目结果。

---

# 9. “非结构化录入、结构化利用”原则

这是 V2.2 的重要技术思想。

录入阶段：

```text
供应商原始资料
→ 一个内容框
→ 保存原文
```

使用阶段：

```text
AI / 规则解析
→ 识别 24口 / 370W / 400万 / 8TB 等信息
→ 用于筛选、匹配和计算
```

因此：

> **不要求用户录入时结构化，但允许系统在需要时智能结构化。**

这是减少用户工作量的关键。

---

# 10. 项目设备选用记录

项目中建立：

```text
ProjectDeviceSelection
```

建议内容：

```text
项目
系统
设备名称
品牌
型号
参数快照
推导数量
人工调整数量
最终数量
项目采用单价
合价
选型来源
选型说明
```

数量逻辑：

```text
自动推导数量（可定义推导方式）
        ↓
人工调整（可选）
        ↓
最终数量
```

费用逻辑：

```text
最终数量 × 项目采用单价 = 合价
```

---

# 11. 设备快速录入

## 11.1 快速添加

页面只保留：

```text
设备名称
单位
通用参数
品牌
型号
型号参数
单价
```

## 11.2 粘贴识别

提供一个“粘贴供应商资料”入口。

例如把完整资料粘进去后，系统尝试识别：

```text
设备名称
品牌
型号
价格
内容
```

用户只需确认，不需要逐项录入。

识别失败时，仍可当作普通内容保存。

---

# 12. 设备中心搜索

搜索必须比传统下拉框强。

支持：

```text
模糊搜索
品牌搜索
型号搜索
内容搜索
系统筛选
常用设备
最近使用
收藏
```

最终目标：

> 3秒内从几千条设备中定位需要的设备。

---

# 13. 设备分类策略

设备分类仍然保留，但不要过度建模。用户可增加减少修改设备分类

推荐：

```text
视频监控
门禁
网络
综合布线
会议
广播
机房
楼控
其他
```

设备名称由设计师自己建立。

不要强制维护复杂的“产品族—产品线—产品子系列”等层级。

---

# 14. 设备选型——可视化定义

选型不再写死。

设计师可以建立：

```text
选型策略
```

例如：

```text
名称：公安项目标准型摄像机策略

候选设备：网络摄像机

条件：
品牌 = 海康 / 大华
参数内容包含：PoE
参数内容包含：400万
参考单价 <= 1500

优先级：
1. 参数满足度
2. 品牌偏好
3. 价格
4. 最近使用
```

系统提供“简易模式”和“高级模式”。

### 简易模式

用可视化条件块：

```text
对象 → 条件 → 优先级 → 结果
```

### 高级模式

允许表达式，但不要求普通用户使用。

---

# 15. 推导——可视化定义

推导采用图形化方式。

例：

```text
[网络摄像机]
       │
       ├── 1:1 → [摄像机支架]
       │
       ├── N:1 → [POE交换机]
       │
       └── N:1 → [NVR]
```

点击一条关系：

```text
来源：网络摄像机
目标：POE交换机

公式：
摄像机数量 ÷ 有效端口数

有效端口数：
端口数 × 80%

取整：
向上取整
```

用户可以拖拽节点、创建连接、修改公式。

---

# 16. 推导规则必须支持四类能力

### 16.1 一对一配套

```text
摄像机 → 支架
1 : 1
```

### 16.2 比例推导

```text
摄像机 → 交换机
24口 × 80% → 有效19台
```

### 16.3 条件推导

```text
如果设备参数包含“室外”
→ 增加防护箱
```

### 16.4 公式推导

```text
录像存储 = 摄像机数量 × 码流 × 保存天数
```

---

# 17. 推导规则的优先级

最终采用：

```text
项目规则
  > 系统规则
  > 用户全局规则
  > 内置默认规则
```

默认规则只负责“能跑起来”，不限制用户修改。

---

# 18. 拓扑——不单独设计数据模型

本次确认：

> **拓扑不是设计数据源，而是系统关系的可视化结果。**

因此不建立独立拓扑数据库。

直接从：

```text
设备关系
+
推导关系
+
选型结果
```

生成：

```text
拓扑视图
```

同一套关系数据，可以提供：

### 推导视图

关注：

> 怎么算。

### 拓扑视图

关注：

> 怎么连接。

这样一套数据支持两种展示。

---

# 19. 设计中心的最终形态

一个系统打开以后：

```text
┌─────────────────────────────────────────┐
│ 视频监控                标准方案         │
│ 点位386  设备12类  推导18条  预算268万    │
├─────────────────────────────────────────┤
│ 概览 | 设计内容 | 点位 | 设备 | 选型 | 推导 │
│ 拓扑 | 工程量 | 清单 | 预算 | 校核 | 说明   │
├─────────────────────────────────────────┤
│                                         │
│               当前工作区                 │
│                                         │
└─────────────────────────────────────────┘
```

---

# 20. 清单与预算

核心链路保持：

```text
点位
 ↓
推导
 ↓
项目设备
 ↓
工程量
 ↓
清单
 ↓
预算
```

清单需要支持：

```text
自动生成
手工调整
批量编辑
版本
差异比较
导出Excel
```

预算需要支持：

```text
项目预算
系统预算
设备预算
品牌切换
方案档次
目标预算
优化方案
```

---

# 21. 预算档次——用户自定义

不要固定只有：

```text
经济
标准
高端
```

用户可以建立：

```text
公安经济型
公安标准型
公安高配型
园区标准型
园区高配型
投标控制价
甲方指定品牌
```

一个项目允许：

```text
默认档次：标准
视频监控：高配
网络：高配
会议：经济
```

---

# 22. 设计工具中心

第一批：

```text
视频存储
视频带宽
POE功率
交换机数量
综合布线
光纤
机柜
UPS
机房功耗
会议容量
广播功率
楼控I/O
```

设计工具必须支持：

```text
计算
预览
写入项目
```

即：

```text
计算器不是孤立工具
↓
计算结果可以直接回写项目
```

---

# 23. 个人工作台

## 23.1 今日

首页优先展示：

```text
今日重点
今日任务
今日日程
项目进度
设计风险
目标进度
习惯
AI建议
```

## 23.2 任务

统一使用一个 Task 模型：

```text
任务
├ 项目任务
├ 系统设计任务
├ 个人任务
├ AI建议任务
└ 目标任务
```

## 23.3 日程

任务解决：

> 做什么。

日程解决：

> 什么时候做。

一个任务允许多个时间块。

## 23.4 目标

```text
年度
→ 季度
→ 月度
→ 周
→ 任务
```

## 23.5 习惯

轻量打卡：

```text
设计学习
阅读规范
技术总结
运动
工作复盘
```

---

# 24. 知识中心

知识不是单纯的文档库。

支持：

```text
规范
产品资料
历史项目
设计经验
技术笔记
模板
```

知识可关联：

```text
系统
设备
项目
任务
模板
```

项目复盘后可沉淀为知识。

---

# 25. 模型与规则中心

新增一级模块：

```text
模型与规则
```

核心包括：

```text
系统模型
设备模型
推导规则
选型策略
清单规则
预算策略
```

但这里必须坚持“可视化、轻量化”。

不让用户直接面对数据库结构。

---

# 26. 开源复用总体策略

本项目不建议寻找一个“万能开源项目”直接 Fork。

正确策略是：

> **核心专业业务自研；成熟通用能力采用开源项目；大型完整应用只借鉴交互与架构，不贸然整包二次开发。**

---

# 27. 推荐开源项目矩阵

| 能力 | 开源项目 | 建议方式 | 备注 |
|---|---|---|---|
| 高级数据表格 | TanStack Table / material-react-table | 直接集成 | 点位、设备、清单 |
| 节点/流程画布 | xyflow/xyflow（React Flow） | 直接集成 | 选型、推导、关系、拓扑 |
| 富文本编辑 | ueberdosis/tiptap | 直接集成 | 设计说明、知识、复盘 |
| 日历 | schedule-x/schedule-x | 直接集成 | 日/周日程 |
| 日历备选 | bigcalendar/react-big-calendar | 直接集成/参考 | React 日历 |
| 类电子表格数据工作台 | gristlabs/grist-core | 重点研究交互/部分能力复用 | 高度适合开放表格思路 |
| 开放数据库/低代码表格 | nocodb/nocodb | 主要研究产品交互 | 整体较重，许可需单独审查 |
| 知识库/工作区 | toeverything/AFFiNE | 主要研究交互 | 项目较大，许可需单独审查 |
| 项目/任务 | makeplane/plane | 主要研究任务模型 | AGPL-3.0，慎重考虑直接二次开发 |

截至本方案编写时，GitHub 信息显示：xyflow/xyflow 为 MIT；ueberdosis/tiptap 为 MIT；schedule-x/schedule-x 为 MIT；bigcalendar/react-big-calendar 为 MIT；gristlabs/grist-core 为 Apache-2.0。citeturn48file0turn51file0turn56file0turn57file0turn53file0

NocoDB 当前 GitHub 元数据显示许可证为 Other/NOASSERTION；AFFiNE 同样显示为 Other/NOASSERTION；Plane 当前显示 AGPL-3.0，因此在实际代码复制、Fork、发布与商业化前必须单独核查各项目仓库的 LICENSE、NOTICE 和具体子目录许可，不应仅依据项目“开源”字样判断可直接二次发布。citeturn49file0turn54file0turn50file0

---

# 28. 开源项目的具体使用方式

## 28.1 TanStack Table

用于：

```text
点位表
设备表
工程量表
清单表
预算表
价格表
```

不要自己实现复杂表格能力。

---

## 28.2 React Flow / xyflow

用于：

```text
设计推导图
设备选型流程
拓扑展示
规则编辑
设备关系
```

这是本项目最值得直接集成的可视化基础设施之一。xyflow 官方仓库当前为 TypeScript 项目、MIT 许可证，并定位为可高度定制的节点式 UI 库。citeturn48file0

---

## 28.3 Tiptap

用于：

```text
设计说明
系统说明
项目复盘
知识笔记
AI生成文本后的编辑
```

Tiptap 当前为 TypeScript 的 headless rich-text editor framework，GitHub 元数据显示为 MIT。citeturn51file0

---

## 28.4 schedule-x

用于：

```text
今日日程
周计划
任务时间块
```

其 GitHub 项目定位为现代事件日历，支持 React 等前端技术，当前为 MIT。citeturn56file0

---

## 28.5 Grist

Grist 的价值不一定是“把整个 Grist 嵌进去”，而是研究：

```text
表格 + 关系 + 公式 + 视图
```

尤其适合参考 RD Workstation 的开放式数据工作台思路。Grist Core 当前 GitHub 元数据显示为 Apache-2.0。citeturn53file0

---

## 28.6 NocoDB

重点研究：

```text
字段配置
表格视图
筛选
排序
分组
关联
```

不建议直接整体 Fork，原因是项目体量很大，而且当前 GitHub 元数据许可证为 Other/NOASSERTION。citeturn49file0

---

## 28.7 Plane

重点研究：

```text
任务
项目
工作周期
看板
路线图
```

不建议把整套 Plane 并入 RD Workstation。当前仓库为 AGPL-3.0，若涉及代码复制、修改后发布等场景，需要严格进行许可证合规评估。citeturn50file0

---

# 29. 技术栈最终确定

```text
TypeScript
React
Vite
Tailwind CSS
shadcn/ui
Zustand
TanStack Query
TanStack Table
React Hook Form
Zod
xyflow / React Flow
Tiptap
ECharts
ExcelJS / SheetJS

Tauri 2
Rust
SQLite
Drizzle ORM
SQLite FTS5
```

---

# 30. 推荐的新数据模型

本项目不再追求“每个参数一列”。

## 核心项目

```text
projects
buildings
areas
telecom_rooms
project_systems
```

## 设计

```text
design_items
points
point_relations
```

其中点位最小字段：

```text
device_id
area_id
telecom_room_id
quantity
```

## 设备

```text
devices
```

建议核心：

```text
id
name
category_id
unit
general_content
```

## 型号

```text
device_models
```

建议：

```text
id
device_id
brand
model
model_content
```

## 价格

```text
prices
```

## 项目选型

```text
project_device_selections
```

包含：

```text
device_id
model_id
snapshot
quantity
unit_price
amount
```

## 规则

```text
design_rules
rule_nodes
rule_edges
rule_conditions
rule_actions
```

这里的结构重点不是让用户维护数据库，而是支持可视化编辑器。

---

# 31. 为什么不能把全部内容都做成 JSON

虽然要求开放，但也不能走极端。

必须保留结构化核心字段：

```text
项目
系统
设备名称
品牌
型号
数量
单价
金额
建筑
区域
弱电间
```

自由内容：

```text
通用参数
型号参数
设计说明
备注
供应商原始资料
```

即：

> **核心数据关系结构化，自由知识内容文本化。**

---

# 32. 设计内容也采用“一个内容框优先”

系统设计说明、设计条件、技术要求等内容，不应该强迫用户分十几个小字段。

推荐：

```text
设计内容
┌──────────────────────────┐
│ 大段设计内容              │
│                          │
│ 可粘贴、可编辑、可AI优化   │
└──────────────────────────┘
```

必要的计算输入才进入参数区。

---

# 33. Engine 设计

保留：

```text
DesignEngine
SelectionEngine
PricingEngine
BillEngine
BudgetEngine
ValidationEngine
SchedulingEngine
GoalEngine
```

增加：

```text
ParseEngine
```

负责从：

```text
供应商参数文本
设备说明
型号参数
```

中识别可利用的信息。

注意：ParseEngine 的识别结果不得直接修改用户原文。

---

# 34. DesignEngine 的最终职责

输入：

```text
点位
设备
规则
项目条件
```

输出：

```text
设计结果
```

例如：

```text
网络摄像机 386
→ POE交换机 21
→ NVR 4
→ 硬盘 48
```

数据库保存结果快照，Engine 负责重新计算。

---

# 35. SelectionEngine 的最终职责

输入：

```text
需要的设备名称

选型策略

品牌偏好
预算
设备内容
```

输出：

```text
候选型号
推荐型号
推荐原因
```

它不能直接覆盖人工已经确认的设备。

所有自动建议都必须有：

```text
采用
忽略
替换
```

三个动作。

---

# 36. ValidationEngine

确定性问题优先由程序发现：

```text
设备缺失
设备无型号
设备无价格
数量异常
规则未命中
存储不足
带宽不足
工程量缺项
```

复杂判断才交给 AI。

---

# 37. AI 应用原则

AI 不允许绕过 Domain Service 直接写库。

```text
AI
 ↓
ContextBuilder
 ↓
结构化建议
 ↓
用户确认
 ↓
Domain Service
 ↓
数据库
```

AI 主要用于：

```text
供应商资料解析
设备推荐
规则建议
设计校核
设计说明
任务排程
项目总结
经验归纳
```

---

# 38. UI/UX 最终设计原则

## 38.1 不做传统 ERP

采用：

```text
专业工作台
+
高信息密度
+
表格
+
侧栏
+
抽屉
+
可视化画布
```

## 38.2 不要频繁弹窗

设备详情、点位编辑、规则设置优先使用右侧 Drawer。

## 38.3 表格就是主要工作区

尤其：

```text
点位
设备
清单
预算
```

## 38.4 快捷输入优先

支持：

```text
粘贴
复制
拖动
Excel导入
快捷搜索
最近使用
```

---

# 39. 推荐页面信息架构

```text
今日
├ 今日重点
├ 日程
├ 待办
├ 项目进度
├ 目标
└ 习惯

项目
├ 项目列表
└ 项目工作台
   ├ 概览
   ├ 建筑
   ├ 系统
   ├ 点位
   ├ 设备
   ├ 任务
   ├ 日程
   ├ 清单
   ├ 预算
   ├ 文档
   ├ 版本
   └ 复盘

设计
├ 系统模板
└ 系统设计工作区

设备
├ 设备列表
├ 分类
├ 品牌
├ 型号
├ 价格
└ 项目选型

清单预算
├ 工程量
├ 清单版本
├ 预算
├ 档次
└ 预算优化

工具
├ 视频
├ 网络
├ 综合布线
├ 机房
└ 其他

知识
├ 规范
├ 设备资料
├ 项目经验
└ 笔记

目标
习惯
AI
模型与规则
设置
```

---

# 40. 第一阶段实施顺序

## P0：重新定义数据与交互

先不新增大量业务。

完成：

```text
设备极简模型
点位模型
建筑/区域/弱电间
项目选型
设备搜索
```

## P1：开放式设计中心

完成：

```text
可配置设计内容
点位表
设备名称链接
快速录入
```

## P2：React Flow 规则中心

完成：

```text
推导节点
连接
条件
公式
结果预览
```

## P3：选型引擎

完成：

```text
候选设备
筛选
优先级
推荐
一键确认
```

## P4：工程量与预算

完成完整链路。

## P5：个人工作台

完成：

```text
任务
日程
目标
习惯
```

## P6：知识与 AI

最后建设智能层。

---

# 41. 第一条必须跑通的完整业务链

以视频监控系统为母版：

```text
新建项目
 ↓
新建A栋
 ↓
建立区域
 ↓
建立弱电间
 ↓
添加视频监控系统
 ↓
点位表
 ↓
选择“网络摄像机”
 ↓
输入区域、弱电间、数量
 ↓
规则推导
 ↓
得到POE交换机/NVR/硬盘
 ↓
智能选型
 ↓
选择品牌/型号
 ↓
生成项目设备
 ↓
生成工程量
 ↓
生成清单
 ↓
生成预算
 ↓
修改点位
 ↓
重新推导
 ↓
对比变化
 ↓
生成新版本
```

**这条链稳定后，再扩展到门禁、综合布线、网络、楼控等系统。**

---

# 42. 数据迁移策略

V1 数据不要丢。

采用：

```text
V1
 ↓
Migration Adapter
 ↓
V2 数据模型
```

重点迁移：

```text
项目
系统
点位
设备名称
品牌
型号
价格
链规则
清单
模板
```

供应商资料与老设备描述尽可能保留原文。

---

# 43. 开源二次开发规范

## 推荐优先级

### A级：直接依赖

```text
TanStack Table
xyflow
Tiptap
schedule-x
ExcelJS
ECharts
```

### B级：研究源码/交互

```text
Grist
NocoDB
AFFiNE
Plane
```

### C级：大型项目暂不整体引入

只有当实际验证发现“重用成本低于自己开发成本”时才考虑。

---

# 44. 开源许可证管理

建立：

```text
third-party/
```

保存：

```text
组件名
GitHub地址
版本
License
使用方式
NOTICE要求
修改情况
```

每次升级依赖时同步检查 License。

特别是 NocoDB、AFFiNE、Plane 这类大型开源应用，不因为“开源”就默认可直接复制代码到产品中。当前 GitHub 元数据分别显示 Other/NOASSERTION、Other/NOASSERTION、AGPL-3.0，必须进行项目级法律与发布策略评估。citeturn49file0turn54file0turn50file0

---

# 45. 当前 RD Workstation 2.0 代码层调整建议

当前 V2.1 已经建立：

```text
features/
domain/拆分后的 types/
services/
engines/
repositories/
seed/
```

下一轮调整重点不是再大规模改目录，而是：

```text
features/
├ device-center/
├ system-design/
├ projects/
├ bills/
└ today/

engines/
├ design.engine.ts
├ selection.engine.ts
├ pricing.engine.ts
├ bill.engine.ts
├ budget.engine.ts
├ validation.engine.ts
├ scheduling.engine.ts
├ goal.engine.ts
└ parse.engine.ts
```

并将开放设计相关逻辑集中到：

```text
services/design.service.ts
services/device.service.ts
engines/design.engine.ts
engines/selection.engine.ts
```

---

# 46. Repository 最终约束

继续使用同步表级泛型 Repository，不创建43个 Repository 类。

但必须完成依赖反转：

```text
Service
 ↓
Repository Interface
 ↓
MemoryRepository
```

不能再：

```text
Service
 ↓
Memory DB
```

另外 Repository 不应暴露整库 `db` 快照作为长期业务 API；Service 应通过受控表操作访问数据。

---

# 47. 测试体系

不优先写大量 UI 自动化测试。

第一优先级测试：

```text
DesignEngine
SelectionEngine
PricingEngine
BillEngine
BudgetEngine
ValidationEngine
```

第二优先级：

```text
ProjectService
DesignService
DeviceService
BillService
```

核心固定测试：

```text
300个摄像机
→ 交换机数量正确
→ NVR数量正确
→ 存储数量正确
→ 清单正确
→ 预算正确
```

这是整个系统的“回归安全网”。

---

# 48. 性能要求

个人工作台的设备数据量预计可以达到数千到数万条。

因此：

```text
设备搜索 → 本地索引 / SQLite FTS5
点位表 → 虚拟列表
设备表 → 虚拟列表
清单表 → 虚拟列表
```

搜索优先本地。

不依赖云端 API。

---

# 49. Tauri + SQLite 最终落地

当 Web 原型业务稳定后：

```text
React
 ↓
Service
 ↓
Repository
 ↓
Tauri Command
 ↓
SQLite
```

SQLite负责：

```text
结构化业务数据
索引
事务
全文搜索
版本
```

本地文件系统负责：

```text
PDF
Word
Excel
图片
CAD
项目附件
```

---

# 50. 项目备份

建议最终形成：

```text
项目名称.rdw
```

包含：

```text
数据库快照
项目附件
设备快照
清单
预算
版本
模板引用
```

支持：

```text
导出
导入
备份
恢复
```

---

# 51. 推荐最终架构

```text
                  RD WORKSTATION 2.0
                           │
             ┌─────────────┴─────────────┐
             │                           │
         个人工作台                    专业设计台
             │                           │
     Goal / Task / Schedule        Project / System
     Habit / Review                Point / Device
             │                           │
             └─────────────┬─────────────┘
                           ↓
                      设计工作流
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
           设备资料       设计规则       项目数据
              │            │            │
              └────────────┼────────────┘
                           ▼
                      Design Engine
                           ↓
                    Selection Engine
                           ↓
                    Bill / Budget
                           ↓
                    Validation / AI
                           ↓
                 Documents / Knowledge
```

---

# 52. 本方案与 V2.1 的关系

V2.1 保留：

```text
React
TypeScript
Vite
Tauri路线
SQLite路线
Services
Engines
Repository
Features
```

V2.2 调整：

```text
设计模型开放化
设备模型极简化
点位模型贴近真实工作
规则可视化
选型可视化
拓扑结果化
开源能力复用化
```

**不是推翻 V2.1，而是纠正 V2.1 在“开放性”和“操作效率”上的产品模型。**

---

# 53. 最终产品判断标准

以后判断一个功能应不应该加入，优先问：

> **这个功能是在帮设计师减少工作，还是增加工作？**

如果一个功能：

```text
字段更多
配置更多
点击更多
重复录入更多
```

即使数据库设计得再漂亮，也不应该进入产品。

反之，如果一个功能：

```text
复制一次
选择一次
批量完成
自动计算
自动关联
自动生成
```

就应该优先开发。

---

# 54. 最终产品定义

> **RD Workstation 2.0 是一个面向弱电智能化设计师的开放式个人工作台：用户负责提供设计内容、供应商资料和设计方法，系统负责搜索、整理、推导、选型、计算、校核、生成和沉淀。**

核心不是“数据库有多少字段”，而是：

```text
录入成本低
搜索速度快
设计过程顺
自动联动强
规则可以自己定义
资料可以持续积累
历史项目可以复用
```

最终目标：

> **让设计师做设计，而不是让设计师维护软件。**

---

# 55. 近期开发路线（正式执行版）

## Sprint 0：产品模型修正

完成：

```text
设备极简模型
点位字段
建筑/区域/弱电间
项目设备选用
```

## Sprint 1：开放式设备中心

完成：

```text
快速录入
供应商资料粘贴
模糊搜索
品牌/型号/价格
项目引用
```

## Sprint 2：开放式点位中心

完成：

```text
设备名称链接
区域联动
弱电间联动
批量编辑
Excel导入
```

## Sprint 3：可视化推导

基于 xyflow：

```text
节点
连接
公式
条件
预览
保存
```

## Sprint 4：智能选型

```text
候选
过滤
优先级
推荐
确认
```

## Sprint 5：清单预算闭环

```text
点位
→ 推导
→ 选型
→ 清单
→ 预算
→ 版本
```

## Sprint 6：个人工作台

```text
任务
日程
目标
习惯
今日
```

## Sprint 7：知识与 AI

```text
知识
资料解析
设计审核
说明生成
智能排程
复盘
```

---

# 56. 第一阶段验收标准

必须同时满足：

```text
□ 一个项目支持多建筑
□ 一个建筑支持多个区域
□ 一个建筑支持多个弱电间
□ 点位只需要设备名称/区域/弱电间/数量
□ 设备名称可模糊搜索
□ 设备资料可直接粘贴
□ 品牌型号可快速录入
□ 型号参数使用一个内容框
□ 数量可以自动推导
□ 数量可人工调整
□ 单价与数量自动得到合价
□ 选型策略可视化编辑
□ 推导规则可视化编辑
□ 拓扑自动生成
□ 清单自动生成
□ 清单可版本比较
□ 预算自动联动
```

达到以上标准后，RD Workstation 才真正进入“专业设计工具”阶段。

---

# 57. 结论

本次方案正式确立三项核心转变：

### 第一：从“固定数据库”转向“轻量开放数据模型”

### 第二：从“程序规定设计方法”转向“设计师可视化定义方法”

### 第三：从“用户录入结构化数据”转向“用户提供内容，系统负责理解和利用”

同时采用“成熟开源能力优先复用、专业弱电能力自主掌控”的研发策略。

这是 RD Workstation 2.0 从一个“有很多功能的工具”真正发展成“弱电设计师个人生产力平台”的关键版本。

---

## 附：本阶段重点参考开源项目

- xyflow/xyflow — React Flow / Svelte Flow，MIT
- ueberdosis/tiptap — Headless rich text editor，MIT
- schedule-x/schedule-x — Event calendar，MIT
- bigcalendar/react-big-calendar — React calendar，MIT
- gristlabs/grist-core — Spreadsheet-oriented data workspace，Apache-2.0
- nocodb/nocodb — Open/self-hosted database workspace，许可证需单独审查
- toeverything/AFFiNE — Knowledge base/workspace，许可证需单独审查
- makeplane/plane — Project management，AGPL-3.0，直接二次开发前需严格评估

---

**文档结束**
