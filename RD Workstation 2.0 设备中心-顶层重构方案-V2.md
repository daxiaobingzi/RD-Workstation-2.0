# RD Workstation 2.0 · 设备中心 顶层重构方案（V2）

> 取代原《方案A：设备主体收敛改造方案》的目录/模型路线（U1 已实施部分保留）。
> 本轮做的是**顶层设计逻辑修改**，非在现有树上缝补：设备主数据的组织主链改为
> **子系统（页签） → 设备类型（主体） → 品牌型号配置行（N）**，
> 类别（前端/后端/管材线缆/辅材…）成为设备类型的属性字段，产品族退出用户心智。
> 技术栈不变（React/TS/内存库/RQ/TanStack/Vite）。

---

## 1. 顶层设计逻辑（目标形态）

```
┌──────────────────────────────────────────────────────────────┐
│  子系统页签：视频监控 | 门禁 | 综合布线 | 信息网络 | 机房 …（17 个） │
├──────────────────────────────────────────────────────────────┤
│  该系统下的 设备类型 主表（按类别分组/筛选）                        │
│    · 高清枪型摄像机   [前端]  品牌备选:海康/大华  型号3  参考价区间  │
│        ├─ 配置行: 大华 DS-2CD1T26DW 经济  ¥420                  │
│        ├─ 配置行: 海康 DS-2CD2646FW 标准  ¥1280                 │
│        └─ 配置行: 海康 DS-2CD7A46   高端  ¥2650                 │
│    · NVR 32路(网络硬盘录像) [后端] …                            │
└──────────────────────────────────────────────────────────────┘
```

### 1.1 实体职责重定义
| 实体 | 职责（顶层模型） | 变更 |
|---|---|---|
| `Product` | **设备类型**：名称/规格/单位/**类别标签**/**子系统**，挂定额材料与数量链 | + `system_id?`（子系统归属）、+ `category?`（类别 code：front/back/cable/aux/net/cee/other）；`product_family_id` 改为可选（引擎过渡期兼容，U4 摘除） |
| `ProductModel` | **品牌型号配置行**：品牌/档位/参数/参考价（一个设备类型下 N 行 = 多品牌备选） | 不变 |
| `DeviceCategory` | 类别字典（前端/后端/管材线缆/辅材/网络/机房…），作为设备类型属性 + 分组/筛选要素 | 字典补齐，不再作为目录层级 |
| `DeviceMaterial` | 设备类型单点定额材料（每点位配比） | 不变（已挂 product_id） |
| `chain_json`（Product） | 设备类型数量推导链（mode/source/factor/reserve/round） | 新增（U4 启用） |
| `design_rules` | 兜底规则（设备链未配置时） | 不变 |
| `product_families` | 引擎内部兼容层 → U4 移除引用 | 表暂存，U4 后不再被用户心智/引擎依赖 |
| `SelectionEngine` | 选型 = **按设备类型（Point.device_id → Product）+ 档次 + 方案规则** 选配置行 | U4 重构（camera 分支模式全 kind 化），废弃 KIND_TO_FAMILY |

### 1.2 主链与 Vue 初版对齐点
- 页签式子系统 = Vue `DatabaseView` 的子系统 tabs；
- 设备类型主表 + 展开配置行 = Vue 设备字典（category 分组） + 价格工作台（设备分组折叠）；
- 参考价主档 + 台账价折叠 = Vue `unitPrice` 直读 + 保留扩展；
- 缺档/缺价/停用被引用的预警 + 按品牌批量调价/品牌替换 = Vue `PriceGovernDialog` + `priceAudit`。

---

## 2. 实施单元（U2 起为新顶层模型）

### U1 ✅（已完成：设备类型复用基础修正，保留）
`resolveDeviceType` / `addDeviceType` / `updateDeviceType` / `removeDeviceType`、`addModel` 两级复用、seed `model_brands` 补齐——均按"设备类型为主体"语义落地，V2 继续沿用。

### U2 · 顶层数据模型落地 + 种子重排（本单元产出：模型 + 数据）
- `types/domain.ts`：`Product` 新增 `system_id?` / `category?`，`product_family_id` 转可选并注释"兼容过渡"。
- `services/device.catalog.ts`：`deviceTypes` 过滤键改为 **systemId + category**（不再按 familyId）；新增 `deviceSystems()`（子系统列表，来自 SYSTEM_GROUPS 常量 + 有数据的系统）；`addDeviceType`/`resolveDeviceType` 支持 `system_id`/`category`。
- `seed` 重排（`seed-devices.ts` / `seed-base.ts` 类别字典）：
  - 类别字典补齐：front/back/cable/aux/net/cee（视频监控下前端/后端/管材/辅材；信息网络下网络；机房下机房类）；
  - 按 17 子系统铺设备类型×配置行（对照 Vue `seeds.js` 设备字典：摄像机/NVR/硬盘/POE交换机；读卡器/门禁控制器/电插锁/出门按钮；信息插座/配线架/理线器；吸顶音箱/功放；UPS/精密空调/机柜；金属线槽/手孔井…）；
  - `product_families` 保留但 products 直接挂 `system_id+category`（family 可为空）。
- 验收：模型/API 编译通过；`vite build` 通过；设备中心按新字段可查询。

### U3 · 设备中心 UI 顶层改造（用户可见的主链落地）
- 左侧栏 → **子系统页签**（17 个，含空态提示"暂无设备"）+ 类别筛选（前端/后端/管材线缆/辅材/网络/机房）。
- 中栏：该子系统 **设备类型主表**（列：设备类型/类别/规格/单位/品牌备选/型号数/档位覆盖/参考价区间/操作）+ **配置行展开**（品牌/型号/档/参数/参考价/状态/操作，沿用 U2 已写的展开交互）。
- 右侧：设备类型详情（规格/单位/类别/品牌 chips/档位覆盖/参考价区间/配置行表/使用情况/定额材料）。
- 型号表单/设备类型表单/品牌表单/参数编辑/批量导入/导出 均已具备，接入新归属字段（`system_id`+`category` 在设备类型表单中选择）。
- 验收：子系统页签切换、类别筛选、展开配置行、改品牌/档/价即时生效、缺价/缺档预警、批量调价/状态、CSV 导出按"子系统-设备类型"命名、Console 无报错。

### U4 · 选型/推导 顶层对齐（数据驱动闭环）
- `SelectionEngine`：移除 `KIND_TO_FAMILY`，选型统一为 **Point.device_id(设备类型) → 其下配置行按 档次+方案规则**（camera 分支模式全 kind 化）；`scheme_rules` 以 kind/category 匹配（`family_id` 字段过渡忽略）。
- `DesignEngine`：数量推导 **设备链优先**（`Product.chain_json`，mode=carry/mul/fixed，source=front/指定设备），未配置回落 `design_rules`；链缺省值由现有 6 条规则初始化，保证首版数值与现状一致。
- demo seed 全链路对齐：点位 `device_id` → 新设备类型 id；选型/清单/预算金额与重构前等价（数值比对）。
- 验收：build/lint；"设计→推导→清单→预算→版本→校核"闭环数值不变；设备中心"数量逻辑"配置区可读可改。

### U5 · 价格治理对齐 Vue（全局治理）
- 台账价（market/supplier/project）保留数据、UI 折叠收纳，参考价主档（型号表单已支持）。
- 新增：**按品牌批量调价**（pct + 取整）、**品牌替换**（停产换主供）、品牌池联动（未收录品牌一键归库）。
- 缺档预警口径：设备类型 × 档位（替代现产品族 × 档位）。
- 验收：治理流程在浏览器走通（体检/调价/替换）、预警条文案正确。

---

## 3. 兼容与风险
| 项 | 处理 |
|---|---|
| seed 全量重排影响 demo 项目 | U4 数值对齐验收（点位/选型/清单金额等价） |
| Product.product_family_id 转可选 | U1 已保证新代码不依赖；U4 摘除引擎引用后完全解耦 |
| 引擎/选型/点位选择器过渡期 | 点位"设备名称"选择器数据源 = 有启用配置行/引用的设备类型（U3 顺带） |
| 在途第三批功能基线 tsc 错误 | 不属于本方案；验收以"本方案涉及文件零错误 + vite build + 浏览器链路"为准 |
| 技术栈 | 不修改（React/TS/内存库/RQ/TanStack/Vite） |

---

## 4. 待确认项
- A. 子系统页签中"无设备"的子系统的显示：全部展示+空态（推荐）vs 只显示有设备的子系统。
- B. 类别标签集合：前端/后端/管材线缆/辅材/网络/机房（推荐）——是否追加"载药/环境"等。
- C. U4 是否彻底移除 `product_family_id`（删除字段 + 迁移 seed），还是保留字段仅解耦引用（推荐后者，U5 后可再决）。
- D. 实施顺序：U2 → U3 → U4 → U5，每个单元验收后再进下一个（默认，确认后开始 U2）。

（等待确认后按 U2 开始实施；确认前不修改代码。）

---

## 5. 实施进度与遗留待办

### 实施进度（2026-08-28）
- U1 ✅ 设备类型复用基础修正
- U2 ✅ 顶层数据模型 + 种子重排（25 设备类型 × 37 配置行，7 个子系统）
- U3 ✅ UI 顶层改造（子系统页签 / 类别筛选 / 设备类型主表 + 配置行展开 / 详情面板）
- U4 ✅ 选型与推导对齐顶层（按设备类型选型；设备链优先且与规则数值等价；数量逻辑面板）
- U5 ✅ 价格治理对齐 Vue（按品牌批量调价+取整 / 品牌替换 / 台账价折叠 / 缺档设备类型×档位 / 导入品牌一键归库）

### 遗留待办（本批已处理，全部闭环，2026-08-28）
- ~~T1. 清单材料行单价口径~~ → **已闭环（批次 C）**：定额材料行（`DeviceMaterial`）新增 `brand/model/params/price`；清单材料行单价优先取定额材料价（如六类双绞线 ¥2.8/m），无价回退旧均价；seed 已补 12 种常用材料品牌/型号/单价（国产/CAT6 等）；材料明细与价格可在设备详情"单点定额材料"中编辑。
- ~~T2. 演示数据重置入口~~ → **已闭环（批次 C）**：设备中心顶部新增"重置演示数据"按钮（两次点击确认 → 清除 `rdw-db-v5` 并重新播种）。
- ~~T3. 第三批功能基线编译错误~~ → **已闭环（批次 C）**：system-design/goals/tasks/topology 等 20 余处类型错误（unused/缺参/Row 超属性/可选字段影响）全部修复，`npm run build`（含 `tsc -b`）恢复整绿，`oxlint` 0 errors。

## 6. 调整批次 B（2026-08-28 后续需求）

按用户 6 点调整完成并通过验收：
- **B1 布局**：内容区 `max-w` 1280px；右侧常驻详情改为**抽屉**（Info 按钮打开，400px 可关），主页面只保留页签+筛选+表格，配色收敛（档次覆盖等改为中性文字样式）。
- **B2 类别**：`DEVICE_CATEGORIES` 移除「网络设备」，POE/核心/接入/汇聚交换机改归「后端设备」（对齐 Vue 四类）。
- **B3 入口**：移除右上角「新增型号」；「新增设备」综合表单（设备：名称/通用参数富文本/单位/子系统/类别 + 配置行：品牌/型号/详细参数富文本/档次/参考价，可加多行一次创建）。
- **B4 折叠图标**：设备行箭头可点（点击行/箭头均可展开收起）。
- **B5 字段模型**：设备=`设备名称 + 通用参数(富文本 HTML) + 单位`；配置行=`品牌 + 型号 + 详细参数(富文本 HTML) + 档次 + 参考价 + 状态`；移除「技术参数(键值对)」与「规格」用于编辑的 UI（`specification` 语义改为通用参数；`parameter_json` 移除，新增 `detail_html`）。
- **B6 批量导入**：模板列 = 设备名称,品牌,型号,档次,参考价,单位,通用参数,详细参数（富文本放最后两列，含逗号用双引号包裹，换行用 &lt;br&gt;；文本域解析器支持引号转义）；设备名不存在则自动新建（归入当前子系统+前端类别）。
- 富文本：引入 `@wangeditor/editor` + `@wangeditor/editor-for-react`（实际使用于通用参数/详细参数）。
- 验收：tsc（本方案文件 0 错误）/ vite build / oxlint 0 errors；浏览器 7 项全通过；Console 仅 wangeditor 编辑器高度警告（非错误）。

（设备中心调整批次 B 完成；遗留待办仍见 §5 的 T1–T3。）

## 7. 调整批次 C（2026-08-28 后续需求）
- **C1 一键展开/收起**：设备主表搜索框左侧加"展开全部/收起全部"。
- **C2 删除设备级联**：`removeDeviceType` 改为级联删除（型号+价格+品牌绑定+档次绑定+定额材料）；其下型号被项目引用的设备**拒绝删除**并弹窗列出"哪款品牌型号被哪些项目使用"（右侧抽屉/行内删除均生效）。
- **C3 定额材料计价（T1）**：`DeviceMaterial` 新增 品牌/型号/参数(单行)/单价；清单材料行价格取定额材料价，规格列显示"品牌 型号"；seed 补常用材料价（国产 CAT6 UTP ¥2.8/m、PVC20 ¥3.5/m、防水盒 ¥18/套 等 12 种）。
- **C4 重置演示数据（T2）**：顶部"重置演示数据"（二次确认）→ 清 `rdw-db-v5` 重播。
- **C5 编译基线清零（T3）**：全仓库 `tsc` 0 错误，`npm run build` 整绿。
- 验收：`npm run build`（tsc+vite）整绿、oxlint 0 errors；浏览器六项全通过（展开全部/级联删除+引用弹窗/材料品牌型号价编辑/清单按材料价计价 34,940×¥2.8=¥97,832/重置入口/Console 无错）。

## 8. 数据列表通用化（react-data-grid v7）—— 已废弃移除（2026-08-28）

> **最终决定：react-data-grid / GridTable 与项目不搭，已全部移除。**
> - `react-data-grid@7` 依赖已卸载（`npm uninstall`）；
> - `components/ui/data-grid.tsx`（GridTable/sortRows）已删除；
> - 所有接入列表（项目中心 / 项目系统 / 系统设计清单 / 项目详情点位表 / 设备中心主表 / 任务列表）均已回滚至各自原自研 Table / TanStack 实现；
> - 本轮临时新增的 `sort_order` 字段与服务排序（Project / Point / Product / Task 等）已随回滚清理，`index.css` 的 rdg 主题也已移除；
> - 当前表格实现回到 2026-08-28 批次 C 收尾状态，`tsc` / `vite build` / `oxlint` 全绿。
> 尝试结论：本项目的自研轻量 Table + TanStack（点位/部分设备场景）与既有抽屉式详情交互更契合，无需引入重型数据网格。