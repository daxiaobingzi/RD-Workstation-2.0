# RD Workstation 2.0 · 模块顶层规划与开源复用方案（V2.3 规划基线）

> 严格以《RD Workstation 2.0 开放式弱电智能化个人工作台实施方案.md》为唯一基线。
> 本文档不实施代码，仅做**顶层模块拆解 + 逐模块开源调研 + 复用建议 + 实施单元 + 验收标准**，供确认后进入实施。

**核心转向（对齐实施方案 §57）**：不再"修补 V2.1 既有代码"，而是按 12 个一级模块重新顶层规划。三个转变：固定数据库 → 轻量开放数据模型；程序规定设计方法 → 设计师可视化定义；用户录结构化 → 用户提供内容、系统理解利用。**成熟开源能力优先复用，专业弱电能力自主掌控**。

---

## 〇、模块总览（实施方案 §3 冻结的 12 个一级模块）

| # | 模块 | 规划定位 | 开源主线（A=直接依赖 / B=研究借鉴 / C=自研） |
|---|---|---|---|
| 01 | 今日 | 个人聚合主页：任务/日程/目标/习惯/项目进度/AI建议 | dasy(MIT)·借鉴widget体系，自研聚合层 |
| 02 | 项目 | 多建筑项目工作台：结构/系统/点位/设备/任务/清单/预算/版本/复盘 | plane(AGPL)·数据模型借鉴 |
| 03 | 设计 | 系统设计工作区：条件/参数/点位/选型/推导/拓扑/清单/预算/校核/说明 | React Flow(MIT)·A / G6(MIT)·A |
| 04 | 设备 | 极简设备库：名称/单位/参数框/品牌/型号/价格/收藏/最近使用 | TanStack Table(MIT)·A；Basmrow/Grist 表格式借鉴 |
| 05 | 清单预算 | 工程量→清单→预算→版本→档次→优化 | TanStack Table(MIT)·A；Invoice Ninja 数据模型借鉴 |
| 06 | 设计工具 | 视频存储/带宽/POE功率/交换机/布线/机房等计算，结果回写项目 | 自研公式内核（仿 formulab 结构） |
| 07 | 知识 | 规范/产品资料/历史项目/经验/模板，关联实体 | Tiptap(MIT)·A；Outline/Trilium 架构借鉴 |
| 08 | 目标 | 年/季/月/周 OKR，手动或数据自动统计 | operately(Apache-2.0)·A |
| 09 | 习惯 | 轻量打卡：连续记录/趋势/目标联动 | Habitica(GPL)·仅借算法 |
| 10 | AI | 嵌入式 AI 面板：解析/推荐/校核/生成，经 Service 落库 | 自研面板；LLM 走统一 Provider 层 |
| 11 | 模型与规则 | 系统/设备模型、推导规则、选型策略可视化编辑 | React Flow(MIT)·A + json-rules-engine(ISC)·A |
| 12 | 设置 | 应用/数据/备份/导入导出 | 自研（轻量配置页） |

**全局数据底座**：Repository 接口（同步）→ 现状 MemoryRepository → 后续 SQLite（Drizzle）替换；TanStack Query 做服务层数据栈；zustand 只做 UI 态。

**许可政策（按"个人自用"设定）**：本软件仅个人使用、不对外分发、不对外提供服务（构成"分发/网络服务"的 copyleft（AGPL）义务与 BSL/SUL/source-available 的商业限制在当前场景基本不触发）。因此：
- **许可不再是一票否决**。高 star 项目可整体自托管并行使用（如 plane、AppFlowy、NocoDB、Grist、Habitica、Trilium、Outline 等）；
- **嵌入当前应用的小型组件仍优先 MIT/Apache**（体积、维护成本最低，且未来即使对外也不受限）；
- 所有采用项仍登记 `third-party/THIRD-PARTY.md`（版本/许可/来源/改动），一旦未来要商用/开源发布，据此重新评估并替换高风险依赖。

---

## 模块 01 · 今日（个人工作台聚合主页）

**顶层设计**：只读聚合 + 一键跳转，不承载编辑逻辑。数据来自 Task/Schedule/Goal/Habit/Project 的跨域查询，按"今日重点/今日任务/今日日程/项目进度/设计风险/目标进度/习惯/AI建议"八栏输出（§23.1）。建议 widget 化：每个区块独立组件、独立缓存、可折叠排序（借鉴 homepage 的 widget 体系思想，但自产自销）。

**开源调研结论**：
- **dashy（≈25.8k，MIT，活跃）→ 借鉴 A**：卡片/分组/布局、主题引擎、YAML schema 校验 —— 是本模块最可深借的底座。
- gethomepage/homepage（≈32k，GPL-3.0）→ 个人自用可直接部署试用，或借鉴其"widget 聚合 + 后端代理隐藏凭据"模式。
- glanceapp/glance（≈36k，AGPL-3.0）→ 个人自用可选，研究轻量布局 DSL 与分源刷新策略。
- 日历渲染复用到模块 02 的 schedule-x（MIT）。流量面板（本机网卡/CPU）个人自用可保留。

**实施单元**：U1 八栏骨架 + 今日任务/日程 + 项目进度条 → U2 目标进度/习惯今天 + widget 折叠 → U3 AI 建议槽位。
**验收**：8 区块全渲染；无编辑状态；点击可跳转对应模块；恩格尔式快速加载。

---

## 模块 02 · 项目（多建筑项目工作台）

**顶层设计**：数据结构 = 项目 → 建筑/building → 区域/area + 弱电间/telecom_room → 系统/point/设备/任务/清单（§5.1）。项目主页聚合：基本信息/进度/系统进度/今日任务/近期日程/预算/设计问题/最近修改（§5.2）。进度不鼓励手填，由任务/点位/清单自动计算。标签页：概览/建筑/系统/点位/设备/任务/日程/清单/预算/文档/版本/复盘。

**子功能拆解**：建筑结构 CRUD（已在点位初版实现）；系统增删；项目任务/日程；清单版本对比；复盘沉淀知识。
**开源调研结论**：
- **plane（≈32k，AGPL-3.0）→ 个人自用 A 可选/整体部署，嵌入用 B 借鉴**：Work Items/Cycles 燃尽图/Modules/多视图（看板/列表/表格/Gantt）/180+ REST API。个人自用可整体部署管项目任务；若嵌入当前应用则借鉴交互与数据模型。
- AppFlowy（≈75.8k，AGPL-3.0）→ 个人自用可整体试用（本地优先 + 多视图数据库）。
- Focalboard（≈26.4k，**Apache-2.0**）→ B：卡片自定义属性可参考；但社区维护、更新停滞，勿依赖。
- operately（≈500，**Apache-2.0**）→ 目标-项目关联 + Check-in 复盘模型，可同时供模块 08。
- 空间结构（建筑/区域/弱电间）无可直接复用的高 star 开源，**C 自研**（领域结构，无通用价值）。

**实施单元**：U1 建筑结构 + 系统 + 点位/设备展示 → U2 任务/日程挂项目 → U3 版本对比 + 复盘知识沉淀。
**验收**：多建筑多弱电间创建/联动选择；进度自动计算；清单版本 diff 可用。

---

## 模块 03 · 设计（系统设计工作区）

**顶层设计**：每个系统统一"概览/设计内容/点位/设备/选型/推导/拓扑/工程量/清单/预算/校核/说明"十二环节（§6.1），但不要求同构——推荐流程可隐藏/显示。点位中心、推导中心、选型中心是同类典型。

**子功能拆解**：点位录入（设备名称+区域+弱电间+数量，已初版）；设备选型（候选/过滤/优先级/推荐/确认，§10）；推导（节点/连接/公式/条件/预览，§11-17）；拓扑（关系结果可视化，§18）；设计内容一个内容框（§32）。
**开源调研结论**：
- **React Flow/xyflow（≈37.9k，MIT，每周发版）→ A 直接依赖**：推导/选型/拓扑画布唯一主力。官方已提供 docs+example，适合做"规则节点编辑器、选型流程图、拓扑视图"。
- workflowbuilder（≈社区新项目，Apache-2.0，基于 React Flow 的 SDK）→ B：学习"节点 schema → 自动属性面板"封装，避免自研胶水。
- **G6（≈12.2k，MIT）→ A（拓扑/关系图）**：与 React Flow 分工——React Flow 做可编辑流程，G6 做静态拓扑/关系可视化；ECharts graph 做大屏概览。
- **Node-RED（24k，Apache-2.0）/ n8n（200k，Sustainable Use）→ 明确 C 提取编辑器**：Node-RED 前端是 AngularJS 1.x，n8n 是 Vue3 且 SUL 限制商业，均非 React 组件，不可嵌入；仅研究其"flow JSON / 节点类型定义"协议设计。

**实施单元**：U1 基于 React Flow 的推导节点编辑器（来源/目标节点 + 公式 + 条件 + 联动预览）→ U2 选型策略画布 → U3 拓扑自动生成（G6）。
**验收**：拖拽建节点、连线、公式/条件可编辑；推导结果与点位/参数实时联动；拓扑由关系数据自动生成，无第二套表（§18）。

---

## 模块 04 · 设备（极简设备库）

**顶层设计**：固定字段仅"设备名称/设备类别/单位/通用参数内容框"（§8.1），品牌/型号/型号参数框/单价为型号层（§8.2），**数量/合价绝对不进库**（§8.3）。快速录入（§11.1）+ 粘贴识别（§11.2）+ 模糊搜索（名称/品牌/型号/内容/系统过滤/常用/最近使用/收藏，§12）。分类保留但不过度建模（§13）。

**子功能拆解**：类别/产品族/产品/型号/品牌/价格四类型；粘贴供应商资料识别；批量导入/批量调价/停用保护；收藏与最近使用。
**开源调研结论**：
- **TanStack Table（≈28-30k，MIT）→ A**：设备/型号/价格表的排序、过滤、行选择、虚拟滚动、单元格编辑全都有，建议用它替换现有手写表格。
- **NocoDB（≈50-62k，SUL）→ 个人自用可选：整体自托管做开放式设备表**（表视图/字段类型/公式/REST API 现成），适合个人数据管理；因是独立 Vue+Nest 应用，不作为当前 React 应用的嵌入依赖。
- **Grist（≈11.5k，Apache-2.0）→ B 研究借鉴**：**公式引擎 + 类 Excel 关系表**是"非结构化录入→结构化利用"的最佳参照；个人自用也可整体自托管试用。
- Baserow（≈9.5k，**MIT**）→ B 借鉴：多视图（网格/看板/表单）+ 公式字段 + API-first 的开放设备表思路。
- 设备搜索可参考模块 11 的表达式引擎做别名/拼音（后续）。

**实施单元**：U1 设备表换用 TanStack Table + 粘贴识别 + 收藏/最近使用 → U2 批量导入/调价 → U3 表格式扩展视图。
**验收**：3 秒内从几千条设备定位设备（§12）；一个内容框录入供应商原文（§9）；数量/合价不在设备库。

---

## 模块 05 · 清单预算

**顶层设计**：链路 = 点位→推导→项目设备→工程量→清单→预算（§20）。数量逻辑 derived/manual/final（§15/§7.2），合价 = final×项目单价（设备库参考价与项目价分离，§7，§8）。清单支持自动生成/手工调整/批量编辑/版本/差异比较/导出 Excel（§20）。预算支持项目/系统/设备分项、品牌切换、档次、目标预算、优化方案（§20）。档次用户自定义（§21）。

**子功能拆解**：清单版本与 diff；预算生成与联动；档次绑定与切换；批量编辑表。
**开源调研结论**：
- **TanStack Table（MIT）→ A**：工程量/清单/预算主表。
- Invoice Ninja（≈9.9k，source-available）→ **B 借鉴其数据模型**：invoice+items+taxes+currency 与"清单+分项+税率+币种"结构高度一致，只抄 ER/API 设计；个人自用也可整体自托管对照体验。
- Crater（AGPL-3.0 且近两年停更）→ 不依赖，仅数据设计可参考。
- 档次/预算优化无可直接高 star 开源 → 自研（对接模块 11 的规则/表达式引擎做优化建议）。

**实施单元**：U1 清单生成 + 版本归档 + diff → U2 预算联动 + 档次切换 → U3 预算优化建议（规则引擎）。
**验收**：300 摄像机链路：POE/NVR/硬盘/清单/预算数量金额全部正确；品牌切换后版本对比可回溯。

---

## 模块 06 · 设计工具（计算器集合，结果回写项目）

**顶层设计**：第一批 12 类计算（视频存储/带宽/POE功率/交换机数量/综合布线/光纤/机柜/UPS/机房功耗/会议容量/广播功率/楼控 I/O，§22）。每个计算器三件套：计算/预览/写入项目（写 design_parameters / project_device_selections）。
**子功能拆解**：公式库；参数表单自动生成；结果预览；写入项目动作。
**开源调研结论**：
- **领域无高 star 专用开源**（弱电计算基本空白，未核实到高质量项目）→ 以**自研公式内核**为主。
- 结构范式参考 **formulab**（TS、零依赖、~200 个工程计算按域拆分，golden-reference 测试）——借用其工程组织，不依赖其库。
- 电气类参考 electrical-calculator-app（NEC/IEC 选型、压降、断路器）与 electrical-calculator-suite（欧姆/三相/电缆估算）的实现方式。
- 公式解析直接用模块 11 的 expr 引擎（jexl/expr-eval 活跃 fork）。

**实施单元**：U1 公式内核 + 视频存储/带宽/POE 三个计算器 → U2 全部 12 类 → U3 参数与项目结果回写。
**验收**：每个计算器独立可用；结果可一键写入项目参数；公式与设计参数联动。

---

## 模块 07 · 知识

**顶层设计**：知识类型 = 规范/产品资料/历史项目/设计经验/技术笔记/模板（§24）；可关联系统/设备/项目/任务（KnowledgeLink）。项目复盘后自动沉淀经验。设计说明用富文本（§11.3）。

**子功能拆解**：知识条目 CRUD + 目录/树 + 全文搜索 + 关联实体 + 富文本编辑/粘贴识别 + 模板组针。
**开源调研结论**：
- **Tiptap（≈37.8k，MIT，v3 每周发版）→ A 直接依赖**：headless 富文本事实标准，`@tiptap/react` + 100+ 扩展（表格/列表/图片），直接做设计说明/知识内容。
- **Lexical（MIT，Meta 维护）→ A 备选**：TypeScript 原生、`@lexical/react`、表格/markdown 插件成熟。
- Plate（≈16.5k，MIT）→ B：50+ 插件，但正处于 Slate→Lexical 迁移期，须锁版本。
- **Outline（≈40k，BSL 1.1）→ 个人自用可直接自托管做知识库**（目录/全文搜索/空间分档体验最佳）；若只在应用内用，则借鉴其交互。
- **Trilium（≈37.5k，AGPL-3.0）→ 个人自用可直接部署**（树+克隆+属性查询+全文搜索），或借鉴其数据模型。
- Logseq/AppFlowy/Joplin（AGPL）→ 个人自用可部署试用，架构研究。
- BlockSuite（AFFiNE 剥离，MIT）→ 后续如需块级文档引擎再评估。

**实施单元**：U1 Tiptap 编辑器 + 知识 CRUD + 类别 → U2 关联实体 + 模板 → U3 全文搜索 + 复盘沉淀。
**验收**：可粘贴供应商/规范原文不留格式损坏；知识可关联项目/系统/设备/任务；设计说明使用富文本。

---

## 模块 08 · 目标（OKR/个人目标）

**顶层设计**：年度→季度→月度→周，可手动可数据自动统计（如"2026 完成 20 个项目"按项目状态自动算，§18）。目标→任务（Goal→Task）关联，任务完成更新目标进度。
**子功能拆解**：目标树；KR 进度（百分比/里程碑）；目标-任务联动；数据自动统计目标。
**开源调研结论**：
- **operately（≈500，Apache-2.0，活跃）→ A 直接依赖**：OKR+项目+Check-ins+Goal-Project 关联，Apache 干净，是目前最完整且可商用的 OKR 模型参照。
- plane 的 Cycles/Modules（AGPL→B）与 AppFlowy 的多视图数据库（B）可作补充视图。
- 专用 OKR 小项目（SoloOKRs 等）低星，参考行为设计不可依赖。

**实施单元**：U1 四级目标 + 手动进度 → U2 目标-任务联动 → U3 数据自动统计目标 + 今日视图露出。
**验收**：目标层级可建；任务完成自动推进目标；数据统计型目标实时正确。

---

## 模块 09 · 习惯

**顶层设计**：保持轻量（§19）：低操作成本、连续记录、统计趋势、目标联动。习惯=名称/频率/目标值；记录=日期/是否完成。
**子功能拆解**：习惯 CRUD + 每日打卡 + 连续天数/完成率统计 + 今日视图 + 目标关联。
**开源调研结论**：
- **Habitica（≈14.1k，GPL-3.0 代码 + 美术 CC-BY-NC-SA）→ 仅借算法（B）**：Habit/Daily/ToDo 三类任务、打卡-连击-等级机制、每日重置状态算法。个人自用也可直接整体部署试用；美术资产若复用仅限个人，不对外发布。
- 其余开源习惯打卡多为移动端/低星 → 自研轻量模块为主。

**实施单元**：U1 习惯 CRUD+打卡+日历热力 → U2 连续/趋势统计+目标联动 → U3 游戏化轻反馈（可开关）。
**验收**：每日打卡 ≤2 次点击；统计图正确；可在"今日"一键完成。

---

## 模块 10 · AI

**顶层设计**：**AI 不允许绕过 Domain Service 直接写库**（§37）：AI→ContextBuilder→结构化建议→用户确认→Domain Service→数据库。AI 嵌入业务页面而非独立聊天（§21）：设备资料解析、选型推荐、规则建议、设计校核、说明生成、任务排程、复盘总结。
**子功能拆解**：统一 LLM Provider 层（模型/密钥/上下文打包）；每类 AI 能力的结构化输出与确认流；AI 面板组件。
**开源调研结论**：
- AI 能力本身非本项目核心开源点，面板与 Provider 层**自研**（复用现有 AIPanel 扩展）。
- 参考：Open WebUI / LibreChat 等聊天壳可自托管作个人 LLM 网关/辅助入口（个人自用无许可障碍），仅借鉴"流式输出/会话管理"模式，不作为应用内嵌依赖。
- 上下文打包复用现有 `engines/ctx` + `services` 的组合（ContextBuilder 定位为 Service 层只读函数）。

**实施单元**：U1 LLM Provider 抽象 + 设备资料粘贴识别 → U2 推荐/校核结构化输出 → U3 说明生成/排程/复盘。
**验收**：任何 AI 写数必经 Service；结构化建议可"采用/忽略/替换"三动作（§35）；断网静默降级不破坏数据。

---

## 模块 11 · 模型与规则（开放式设计方法中心）

**顶层设计**：可视化、轻量化（§25）。四块：推导规则、选型策略、清单规则、预算策略；系统/设备模型定义。规则优先级：项目规则>系统规则>全局>内置 fallback（§17，已实现于 engines/default-rules.ts）。规则四类能力：1:1/比例/条件/公式（§16）。

**子功能拆解**：节点编辑器（来源/目标/公式/条件/取整）；策略编辑器（候选设备/条件块/优先级）；规则绑定（项目/系统级覆盖）；表达式/公式校验与预览。
**开源调研结论**：
- **React Flow（MIT，37.9k）→ A**：规则节点画布（与模块 03 共用基础）。
- **json-rules-engine（ISC）→ A**：规则即 JSON，天然可存库/可视化，浏览器/Node 双端。
- **jexl（用活跃 fork @jbrowse/jexl，MIT）→ A/B**：表达式引擎，做"规则里的取值/计算表达式"；expr-eval 的 fork @expr-eval/js 做纯数学公式。
- workflowbuilder（Apache-2.0）→ B：节点 schema 驱动属性面板的封装范式。
- nools → C（已弃坑）。

**实施单元**：U1 规则节点编辑器（React Flow）+ 表达式引擎接入 + 现有 DesignEngine 联调 → U2 选型策略编辑器 → U3 规则绑定/覆盖 + 预览与校验。
**验收**：不写 JSON 即可建立一条推导/选型规则（§10-11）；修改规则后重新推导结果正确；规则可项目级覆盖。

---

## 模块 12 · 设置

**顶层设计**：应用外观、数据管理（导出/导入/重置）、备份（方案 §50 的 .rdw 项目包）、开源依赖清单（§44 third-party/）、关于。保持轻量，不做复杂配置（§38.2 反模式）。
**子功能拆解**：常规设置表单；数据导出/导入 JSON；备份/恢复骨架。
**开源调研结论**：无高价值通用开源需要引入（配置页属普通表单）；可借鉴 dashy 的 YAML schema 校验交互做"以文件形式的设置导入导出"（B）。
**实施单元**：U1 数据导出/导入/重置 + 外观选项 → U2 备份/恢复骨架 → U3 third-party 清单展示。
**验收**：导出导入不丢业务数据；重置确认两级；第三方许可清单自动展示。

---

## 一、全局架构顶层设计（跨模块）

1. **数据访问**：Repository 接口（同步）不变，Memory → SQLite(Drizzle) 平滑替换；Service 仅依赖接口（§46）。
2. **状态管理**：TanStack Query 负责服务层数据（缓存/失效），zustand 仅 UI 态；消除"组件直接操作 db"（code review 红线）。
3. **UI 基础**：现有 components/ui 库保留并扩展；表格一律迁移 TanStack Table；日历/富文本/画布全部走 MIT 依赖，国产化自研不重复造轮子。
4. **许可管理**：建 `third-party/THIRD-PARTY.md` 登记每个依赖的版本/许可/来源/改动（§44）。**当前"个人自用"设定下许可不做一票否决**；凡整体部署的 AGPL/SUL/BSL 项目单列"个人自用可选"条目，一旦未来对外发布再逐项评估替换。

## 二、实施顺序（依赖驱动）

- **M0 数据底座**：TanStack Query 接入 + Repository 接口收敛（无 UI 变化）。
- **M1 核心即战链路（视频监控母版完整闭环）**：模块 03/04/05/11 联调——点位→推导→选型→清单→预算全链稳定（§41）。
- **M2 个人工作台**：模块 01/02/08/09（今日/项目/目标/习惯）。
- **M3 生产力扩展**：模块 06/07/10/12（工具/知识/AI/设置）。
- **M4 精度打磨**：全链路版本对比、性能（虚拟列表）、检索（FTS5 迁移）。

## 三、下一步（待确认项）

1. 确认本 12 模块分解与实施顺序是否符合预期；
2. 确认第一优先级是"完整跑通视频监控母版闭环"，再谈个人工作台；
3. 确认 M0 是否允许先引入 TanStack Query（架构层、无 UI 变化）；
4. 确认 AI 模块的 LLM 通道（本地/云端/预留接口）。

**本文档为规划基线，未经确认不实施任何代码。**